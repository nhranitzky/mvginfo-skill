[project]
name = "mvgcli"
version = "1.0.0"
description = "Runtime dependencies for the mvgcli skill wrapper"
requires-python = ">=3.11"
dependencies = [
    "mvg>=1.6.0",
    "playwright>=1.40",
    "pydantic>=2.7",
    "rich>=13.0",
    "typer>=0.12",
]

[tool.uv]
package = false
