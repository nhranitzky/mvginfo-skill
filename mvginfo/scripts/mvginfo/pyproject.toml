[project]
name = "mvginfo"
version = "1.0.0"
description = "Runtime dependencies for the mvginfo skill wrapper"
requires-python = ">=3.13,<3.14"
dependencies = [
    "async-mvg-api>=0.3",
    "pydantic>=2.7",
    "typer>=0.12",
]

[tool.uv]
package = false
